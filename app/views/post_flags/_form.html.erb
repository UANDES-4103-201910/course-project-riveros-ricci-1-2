<%= form_with(model: post_flag, local: true) do |form| %>
  <% if post_flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_flag.errors.count, "error") %> prohibited this post_flag from being saved:</h2>

      <ul>
      <% post_flag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :flagged_by_id %>
    <%= form.text_field :flagged_by_id %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
