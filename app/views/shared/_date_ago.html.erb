<%# Usage: <%= render partial: 'shared/date_ago', locals: {html_class: 'text-muted float-right', date: comment.created_at} \%\> %>
<div class="<%= defined?(html_class) ? html_class : "text-muted" %>"
     data-toggle="tooltip" data-placement="top" title="<%= date.to_formatted_s(:rfc822) %>">
  <%= distance_of_time_in_words(DateTime.now, date) %> ago
</div>