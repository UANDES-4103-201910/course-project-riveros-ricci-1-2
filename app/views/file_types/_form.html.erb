<%= form_with(model: file_type, local: true) do |form| %>
  <% if file_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file_type.errors.count, "error") %> prohibited this file_type from being saved:</h2>

      <ul>
      <% file_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image_path %>
    <%= form.text_field :image_path %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
