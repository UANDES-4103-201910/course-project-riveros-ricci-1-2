<%= form_with(model: user_status, local: true) do |form| %>
  <% if user_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_status.errors.count, "error") %> prohibited this user_status from being saved:</h2>

      <ul>
      <% user_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
